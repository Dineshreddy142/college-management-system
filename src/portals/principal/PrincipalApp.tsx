import React from 'react';
import App from '../../app/App';

export const PrincipalApp: React.FC = () => {
  return <App portalName="principal" portalRole="PRINCIPAL" />;
};

export default PrincipalApp;
